# CLAUDE.md 工作流（每轮对话强制执行，严禁忽略）

> ⚠️ 本工作流**每一轮对话都强制生效**，与是否启用「角色分工与协作规范」无关。  
> 区别仅在于「**谁来改 CLAUDE.md**」：本轮由 Claude 写代码 → Claude 自己改；本轮由 Codex 写代码 → Codex 改。读取动作（回答前那一段）**永远由 Claude 自己执行**。  
> **本规则与「禁止自己写代码（角色分工启用时）」「禁止跳过审查清单」属于同一最高优先级，违反即违规。**

## 回答前必须执行（每轮对话强制，无例外）

**优先级说明：本节是回答用户前的第一道关，先于任何思考、任何工具调用、任何回复内容。漏掉视为严重违规。**

1. **识别本轮对话涉及哪些项目**。判断依据按以下优先级，从高到低取并集（不是择一）：
   - 用户消息正文中**明确提到**的项目名（如 `bmec-end`、`bmec-pc`、`bmec-mall`、`sitc-app-end` 等）
   - 用户消息中**出现的绝对路径或目录片段**（如 `/Users/zoujunyong/IdeaProjects/hzr/xxx`）
   - 用户**附图/附文件**所在目录的项目根
   - 当前工作目录（cwd）所在项目
   - 上轮对话已经确立的项目上下文（若用户没切换话题）
   
   ⚠️ **特别注意**：cwd 只是兜底依据，**不是唯一依据**。用户经常在 A 项目启动 Claude，但本轮在聊 B 项目——此时必须以用户消息内容为准，不得只读 cwd 对应项目的 CLAUDE.md 就回答。
   
   ⚠️ **不确定时不得猜**：识别不出来或拿不准是哪个项目，**必须先问用户**「本次任务涉及哪个项目？路径是什么？」，禁止自行假设后推进。

2. **用 Read 工具读取**每个涉及项目根目录下的 `CLAUDE.md`。
   - 即使本会话之前读过，**本轮也要重新读一次**——外部进程（Codex、用户手改、其它窗口）可能已修改文件，上下文里的旧版不作数
   - 多项目涉及就**多次 Read**，禁止偷懒只读一个
   - Read 路径必须是该项目根目录下的 `CLAUDE.md`，不是项目子目录里的同名文件
   - 若项目根存在 `PROJECT_NOTES.md` 而无 `CLAUDE.md`，按下文「向下兼容规则」处理后再读

3. 若某项目根目录下不存在 `CLAUDE.md`：
   - 本轮由 Claude 写代码 → Claude 自己创建
   - 本轮由 Codex 写代码 → 通过 Codex 创建
   - 创建内容必须含：项目名称、技术栈、业务逻辑、重要接口与约定、注意事项

4. 回答与实施时严格遵守各自项目文档约定，**不得把 A 项目约定套到 B 项目**。多项目同时改动时，每项操作前都要回头确认是按哪个项目的规则在做。

5. **回复末尾必须注明本轮读取了哪些项目的 CLAUDE.md**（格式：`已读取 CLAUDE.md：<项目1>、<项目2>` 或 `本轮未读取 CLAUDE.md：<原因>`），让用户能直接验证我是否执行了本规则。

6. **唯一允许跳过的例外**（仅以下三种，其余一律不许跳）：
   - 纯闲聊、问候、与代码完全无关
   - 询问 Claude Code 自身用法、全局规则、hook 配置等元问题
   - 用户本轮明确豁免（如「这次不用读 CLAUDE.md」）
   
   一旦本轮涉及**任何**项目的代码、配置、脚本、文档改动或答疑，例外不成立，必须执行 1–5 步。

## 严禁行为（违反即违规，与「禁止自己写代码」「禁止跳过审查清单」同级）

❌ 严禁以「上一轮已经读过」「会话里读过了」为由跳过本轮 Read——必须每轮重读
❌ 严禁仅凭 cwd 推断项目，忽视用户消息中明确提到的其它项目
❌ 严禁同时涉及多项目时只读其中一个
❌ 严禁不读 CLAUDE.md 就开始回答、开始调工具、开始派 Codex
❌ 严禁省略回复末尾的「已读取 CLAUDE.md：...」声明
❌ 严禁识别不出项目还自行假设推进——不确定必须先问

## 任务结束后必须执行

执行者按本轮谁写代码区分:

- **本轮由 Claude 写代码** → Claude 自己更新对应项目 CLAUDE.md
- **本轮由 Codex 写代码** → 任务派发指令里必须包含「按本规范更新对应项目 CLAUDE.md」要求，由 Codex 完成
- **本轮无人写代码（纯讨论/解释）** → 不需要更新

1. 对本次**实际发生修改**的每个项目，分别增量更新其根目录的 `CLAUDE.md`：
   - 新增/变更的业务流程、页面职责
   - 新增/修改的接口、参数、字段说明
   - 重要技术决策、命名约定、易踩坑点

### 判断是否属于业务变更（需要更新文档）

**需要更新：**
- 接口行为变化（返回数据范围、过滤条件、字段含义改变）
- 业务规则新增或调整（如：只返回双上架商品）
- 数据流/处理逻辑变更（如：同步时删 ES 的条件）
- 重要约定（字段命名、状态枚举含义、跨模块联动）
- 易踩坑点（隐式过滤、特殊判断逻辑）

**不需要更新：**
- 纯代码优化，行为完全不变（如：两次相同查询合并成一次，结果不变）
- 编译/格式/环境问题修复
- 日志调整、注释修改

**执行时机（严格）**：任务完成后**立即**更新，不得用"稍后更新"等措辞延迟。不能依赖用户再次发消息来触发更新。

2. **严禁跨项目混写**：改了哪个项目就只更新那个项目的文档。
3. 前后端都改了 → 两个文档分别更新，禁止只更新一个。
4. 回复末尾必须注明：`已更新 CLAUDE.md（列出已更新的项目）`；若未更新须明确说明原因。

## 每次任务结束前检查清单

- [ ] 是否识别并读取了本次涉及的所有项目的 `CLAUDE.md`
- [ ] 本次涉及的业务逻辑是否已分别写入对应项目文档
- [ ] 坑点和歧义点是否已分别写入对应项目文档
- [ ] 每个被修改项目的 `CLAUDE.md` 是否已保存
- [ ] 回复末尾是否已注明「已更新 CLAUDE.md（项目列表）」

## 文档规范

- 文件名：`CLAUDE.md`
- 位置：各项目根目录（与 `package.json` 或 `pom.xml` 同级）
- 一项目一文档，多项目禁止共用
- **`CLAUDE.md` 是技术架构说明与业务逻辑文档的统一文件**，分两部分：
  - **技术架构部分**：由 `/init` 生成，供 Claude Code 读取（构建命令、架构说明等），**不得覆盖或修改**此部分（Claude、Codex 均同）
  - **业务逻辑部分**：**谁写代码谁维护**——本轮 Claude 写代码则 Claude 维护，本轮 Codex 写代码则 Codex 维护；在 `## 业务逻辑文档` 章节下增量更新

## 业务逻辑文档写入规则（强制，对维护者通用）

无论维护者是 Claude 还是 Codex，更新 `CLAUDE.md` 时必须遵守：

1. **文件已存在**（如由 `/init` 生成）→ 默认只在文件末尾追加或更新 `## 业务逻辑文档` 章节；**技术架构部分（该章节以上的内容）除非用户明确指令要求修改，否则不得触碰**
2. **文件不存在** → 创建文件，内容只包含 `## 业务逻辑文档` 章节
3. **写作准则（每条内容落笔前都要自问一遍）**：
   - 写「为什么」，不写「是什么」——「是什么」代码里能读出来，「为什么」读不出来
   - 写反常识的、跨文件的隐式联动——和直觉一致的不必写
   - 一条内容会被引用 ≥5 次才值得进文档；只用一次的别写
   - 过期内容**标 `DEPRECATED` 不删除**——删了下次还会有人提一样的方案。状态值：`ACTIVE` / `DEPRECATED`
   - 已能从 `git log` / 实体类 / Controller / `ls` 直接读到的信息**禁止再抄一遍**（changelog、完整接口列表、字段表、目录树等）

4. `## 业务逻辑文档` 章节的标准结构（按优先级排列，**P0 必写，P1 强烈建议，P2 按需**；空章节可省略不写）：

```markdown
---

## 业务逻辑文档

> 此章节由本轮写代码者维护（Claude 或 Codex）。
> 准则：写「为什么」不写「是什么」；写反常识与跨文件隐式联动；过期标 DEPRECATED 不删除。

### [P0] 项目身份
- 项目名 / 技术栈+主版本号（如 Spring Boot 2.7、Vue 3） / 默认端口
- 本地启动命令、构建命令

### [P0] 业务术语表
（5–20 个；一句话定义，让阅读者不会把字段名理解错）
- `术语`：含义

### [P0] 隐式规则与坑点 ★最关键
（藏在代码里、grep 看不出意图的约定。本节缺失是绝大多数 Bug 的根源）
- 规则描述 / 涉及模块 / 状态：ACTIVE

### [P0] 核心业务流程
（3–5 条主链路；只画流程，禁止贴代码）
- 流程名：A → B → C，关键判断点说明

### [P1] 目录地图
（哪个 module / 包负责什么；入口在哪。粒度到「模块/职责」一行，不抄目录树）

### [P1] 技术决策记录
（为什么这么选 + **放弃方案及原因** ← 防同方案被反复提）
- 决策 / 背景 / 放弃方案 / 状态：ACTIVE

### [P1] 接口约定
（命名风格、统一返回结构、错误码段位、分页参数名等。一条样例即可，禁止罗列全部接口）

### [P1] 历史 Bug 复盘
（只记**下次还会再踩**的，流水账禁止）
- 现象 / 根因 / 解法 / 关联模块 / 状态：ACTIVE

### [P2] 代码风格约定
（日志、异常、空值处理风格；与通用规范不同的部分才写）

### [P2] 环境差异
（本地/测试/生产配置差异点；密钥从哪取，禁止写密钥本身）

### [P2] 外部依赖坑点
（第三方 SDK 版本、协议踩坑）

### [P2] 数据库关键约束
（核心索引、唯一约束、跨表隐式联动如「删 A 必须清 B 缓存」）
```

5. **禁止写入**（写了反而干扰）：
   - 完整接口文档、完整字段表、完整目录树（用代码/`ls`/`tree` 替代）
   - changelog / 变更时间线（用 `git log` 替代）
   - 通用编程规范（如「代码要写注释」这类废话）
   - 个人偏好（放全局 `~/.claude/CLAUDE.md`，不放项目）
   - DEPRECATED 旧方案的大段保留——只留一行指针：「曾用 X，已弃用，原因见 [决策记录-XX]」

## 向下兼容规则（强制）

已有项目中存在 `PROJECT_NOTES.md` 时，**必须先将其重命名为 `CLAUDE.md`，再进行任何读写操作**：

1. 检测到项目根目录存在 `PROJECT_NOTES.md` → 命令 Codex 执行重命名（`mv PROJECT_NOTES.md CLAUDE.md`）
2. 重命名完成后，后续统一使用 `CLAUDE.md`，不再引用旧文件名
3. 重命名本身属于文件操作，同样交给 Codex 执行，我不得直接操作
