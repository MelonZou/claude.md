# 代码审查检查清单

> 每次 Codex 完成后，审查前必须读取此文件，逐项执行。持续积累，随时更新。

## 清单维护规则（新增检查项前必读）

发现的问题要写成清单里的一条检查项，须同时满足：

- **是模式，不是孤立笔误**：该缺陷代表一类可能在其他文件/组件里以同样形态重现的错误，不是仅此一处、以后不会再犯的一次性问题
- **正常读 diff 容易漏**：单独看修改点时逻辑看似合理，只有专门带着这条检查项去验证才能发现；能被自然读码发现的不必单列
- **可执行**：写成「检查 XXX 是否 YYY」这种具体动作，而不是"代码要写好"之类无法落地的原则
- **表述要泛化**：抽象成通用模式（字段名/组件名可以举例但不要绑死），否则以后遇到不同场景用不上

不满足就不写，避免清单越堆越长、逐项执行变成走过场。

---

## 通用检查

- [ ] `git diff --stat HEAD` 确认所有改动文件范围，不遗漏
- [ ] **改动涉及页面/组件时，必须往上追溯入口**：确认实际调用方（路由跳转、父组件引用）与改动文件一致，防止改了不会被触发的文件。具体做法：grep 改动文件名在整个项目中的引用，并确认入口传参（如 `currentType`）与改动中的条件判断匹配
- [ ] 新增方法：null 参数处理、边界条件是否覆盖
- [ ] 异常类型是否符合项目规范（Spring Boot 项目用 `RcBizException`，禁止抛 `IllegalArgumentException` 等原生异常）；捕获到框架/驱动抛出的原生异常（如 `DataIntegrityViolationException`、`PSQLException`）时是否也统一转换为 `RcBizException`，不能直接冒泡或把异常堆栈/SQL 语句透出给前端
- [ ] SQL / Mapper：参数为 null 时语义是否正确（PostgreSQL `col = NULL` 永远 false，需 `<if test>` 处理）
- [ ] 跨表写操作是否加 `@Transactional`
- [ ] 按 ID 直接查询/操作资源的接口（如 `/xxx/{id}`）是否校验了资源归属（`companyId`/`employeeId` 等）与当前登录身份一致，防止越权访问/修改他人数据（IDOR）
- [ ] 日志打印、异常响应（含返回给前端的错误信息）中是否透出了密码、token、身份证号等敏感字段原文
- [ ] 批量处理逻辑中是否存在循环内对每条记录单独查询 DB/远程接口（N+1），应改为批量查询后在内存按主键/编码建 Map 匹配
- [ ] 新增配置或代码中是否出现明文密码、密钥、Token（未走 Jasypt 加密或环境变量注入）
- [ ] 新增/修改字段的防御性判空或必填校验时，是否已核实该字段在**其它合法消息类型/业务分支**下不存在天然为空的情况（如状态相关的精简消息、可选字段等）？代码逻辑本身自洽无法证明校验合理，无法从代码确认时必须查生产历史数据验证（如 SKU 下架精简消息不带 `sku_name` 却被误判为脏数据的历史教训）
- [ ] 通过「目标是否出现在某个明细/错误列表里」反向判断状态（如「不在错误列表里就算通过/可售」）的逻辑，是否验证过该列表为空时的行为：外层已明确表示整体失败/异常时，列表为空不能被当成「全部通过」，需要有专门分支保底为不通过（bmec-end `ZtApiService#checkSkusCanSale` 曾因中台失败但 `data=[]`，导致所有 SKU 被误判为 `canSale=true` 的教训）

---

## Spring Bean 专项（强制，无例外）

**触发条件（任一成立即必须执行）**：
- 新增 @Service / @Component / @Repository
- **已有 Service 新增 / 修改了任何注入字段**（不论是构造注入 `private final` 还是 `@Autowired` 字段注入）
- 已有 Service 改了 setter 注入或 @Resource 注入

> ⚠️ "已有 Service 加新依赖" 是循环依赖最高发场景，比新建 Bean 更隐蔽，**必须重点查**。本规则曾因触发条件只覆盖"新增 Bean"而漏检，2026-05-12 扩大覆盖到所有依赖变更。

**必查项**：

- [ ] 列出本次改动涉及的每个 Bean 的全部注入依赖（构造 `private final` + `@Autowired` 字段 + setter / @Resource，全部列）
- [ ] **显式画出 A→B→A 的依赖图**，存在环 → 必须打回。Spring Boot 2.6+ 默认 `allow-circular-references=false`，环存在即启动失败
- [ ] 检查混合注入（同一 Bean 同时存在构造注入 + 字段注入）：构造环可能直接启动失败；字段环靠延迟初始化可能侥幸通过，但仍属违规，一并打回
- [ ] 解环优先方案：把 Service→Service 改成 Service→Mapper（Mapper 不参与 Service 互引环）

---

## 前端 Vue 专项（有新增/修改 Vue 组件时）

- [ ] 同类型多个 Tab / 表格中，`fixed="right"` 等样式属性是否一致
- [ ] API 调用失败是否有 `ElMessage.error` 提示，不能静默失败
- [ ] 关键操作（删除/移除）是否有二次确认弹窗
- [ ] 表格/网格用「外层容器边框 + 内部单元格边框」组合模拟完整边线时（常见于 CSS grid 自绘表格），检查容器边框与最后一行/列的边框声明是否相互抵消——容器本该补位的那条边被自己设成 `none`，内部单元格又指望容器补位，导致该边整体没画出来（2026-07-07 bmec-mall 包裹商品表底边/右边缺失即此模式）
- [ ] 新增/修改 `v-html` 绑定时，渲染内容是否用户可控且未经净化处理，存在 XSS 风险
- [ ] 列表页新增列排序（`sortable="custom"` 等）或其他独立于 `searchForm` 的筛选/交互状态时，检查已有的「重置」函数是否同步清空了这份新状态（含 Element Plus 组件自身的 UI 状态，如表格排序箭头需调用 `tableRef.value?.clearSort()`），不能只清 `searchForm`

---

## MyBatis XML 专项

- [ ] `<foreach>` 集合为空时是否会生成非法 SQL（如 `IN ()`）
- [ ] 结果映射 resultMap 是否覆盖所有新增字段
- [ ] 新增查询方法是否在 Mapper 接口中声明
- [ ] 是否误用 `${}` 而非 `#{}` 拼接用户可控参数，存在 SQL 注入风险（排序字段、动态表名等确需 `${}` 时是否做了白名单校验）

---

## 数据库变更专项（有 DDL 改动时必查）

- [ ] 涉及数据库结构变更（新增字段、新建表、改列类型等）时，必须用 Python 连接目标数据库确认变更已实际执行，不能仅依赖 DDL 文件或编译通过
